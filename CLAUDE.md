# AI 编码约束

## 本项目是纯前端的项目： HTML+CSS+JavaScript+部分文档

## 任务工作方式与提交
- 需求 / 场景不清晰 → 先问 2-3 个核心问题；大型功能先给设计方案，用户确认后再写代码；**PRD 已有设计方案就不重复设计**。
- 方案评审需分析逻辑漏洞 / 架构 / 能否落地；
- Bug 修复走「复现 → 定位 → 修复 → 验证 → 固化」，固化即补测试用例。
- Git：生成提交信息后**由用户审核并手动提交，不要自动 commit**，按类型分组、每个小点足够简洁，不超过20字 —— 
  - `feat:` python 功能 · `fix:` 修复的 bug · `docs:` 文档。
  - 例如：
  ```text
  feat: 
    - python 功能1
    - python 功能2
  fix: 
    - 修复的 bug1
    - 修复的 bug2
  docs:
    - 文档1
    - 文档2
  ```


## 禁止行为

> 本层每条都是硬约束。与前面各层冲突时，以本层为准。

### 禁止猜接口与臆造业务规则
- **禁止行为**：接口签名、字段含义、业务规则不得由推测得出；不确定时先提问，拿到答复再动手。
- **示例**：反例 `# 自行假设 get_user() 返回哪些字段`；正例 `# 先问清字段清单，再写解析`

### 禁止扩大修改范围
- **禁止行为**：只改与本次目标相关的文件与函数；顺手重构、顺手统一风格都不属于本次目标。
- **示例**：反例 `# 修 A 的缺陷时顺手重排 B 的 import`；正例 `# 把发现的 B 问题写成一条新任务`

### 禁止破坏既有架构边界
- **禁止行为**：不绕过既有抽象层，不改依赖方向，不为省事直接取用下层资源。
- **示例**：反例 `# 在接口层直接执行 SQL`；正例 `# 调用服务层已暴露的方法`

### 禁止过度工程
- **禁止行为**：不为简单需求设计抽象层、插件机制与配置项；不为「将来可能需要」预留参数。
- **示例**：反例 `# 为单个调用点写一个策略类加注册表`；正例 `# 先写一个函数，等第二个调用点出现再抽象`

### 禁止只修表面症状
- **禁止行为**：先说清根因再改代码；症状相似不等于根因相同。
- **示例**：反例 `# 捕获异常让报错消失`；正例 `# 定位到产生该异常的分支并修正条件`

### 禁止提交凭据与死代码
- **禁止行为**：密钥、口令、令牌、个人绝对路径不进代码与配置；被注释掉的代码不留。
- **示例**：反例 `# API_TOKEN 直接写进源码`；正例 `# 运行时注入，源码里只留读取处`